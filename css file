/* background-size: gives a size */
body {
    color: black;
    text-align: center;
    font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif; /* Change to a bold, flashy font */
    font-size: 15px;
    background: linear-gradient(-45deg, white, #ff9c2b);
}

/* Animations! for colour flow */
@keyframes gradientMove {
    0%{ background-position: 0% 50%; }
    50%{ background-position: 100% 50%; }
    100%{ background-position: 0% 50%; }
}

h1 {
    font-family:"Palatino Linotype", "Book Antiqua", Palatino, serif;
    font-weight: 800;
    font-size: 130px; 
    text-align: center;              
    text-shadow: 3px 3px 5px rgba(0, 0, 0, 0.5); /* Add a shadow effect */
    animation: glow 1s infinite alternate; 
    position: relative;
    z-index: 1;
    color: rgb(255, 165, 47);
    letter-spacing: 8px;
}


/* Navigation Bar (1) */
nav {
    background: rgb(255, 60, 0);
    padding: 19px;
    text-align: center;
    border-radius: 20px;
}

/* Navigation bar at top */
nav button {
    background: linear-gradient(45deg, #ffef41, #ff4b2b);
    color: white;
    font-size: 18px;
    font-weight: bold;
    border: none;
    border-radius: 25px;
    padding: 12px 20px;
    margin: 10px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;/*0.3 s long transition*/
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);/*Shadows!*/
}

/* Hover! */
nav button:hover {
    background: linear-gradient(45deg, #ffea2b, #ffef41);
    transform: scale(1.1);
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.5);
}


nav {
    background: rgb(255, 94, 0) ;
    padding: 15px;
    text-align: center;
    border-radius: 10px;
    
}
nav button {
    background: linear-gradient(45deg, #ffef41, #ff4b2b);
    color: white;
    font-size: 18px;
    font-weight: bold;
    border: none;
    border-radius: 25px;
    padding: 12px 20px;
    margin: 10px;
    cursor: pointer;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);

}

/* Footer */
footer,p {
    margin-top: 20px;
    background-color: #7d692e;
    color: white;
    padding: 15px;
    text-align:left;
    border-radius: 10px;
}

/* Footer on text */
footer p {
    font-size: 15px;
    opacity: 0.8;
    text-align:left;
}

p{
    font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif; 
    font-size: 18px; 
    text-align:left;

}

.section {
    text-align: center;
    padding: 20px;
    align-items: center;
}

.buttons {
    justify-content: center;
    gap: 20px;
    align-items: center;
}
.button {
    background: linear-gradient(45deg, #ffef41, #ff4b2b);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 30px 50px;
    margin: 10px;
    border: none;
    background-color: #ffff00;
    color: white;
    font-size: 24px;
    cursor: pointer;
    border-radius: 10px;
    width: 300px;
    height: 200px;
}
.button:hover {
    background-color: #b3a700;
}
.button img {
    width: 80px;
    height: 80px;
    margin-bottom: 10px;
}
.content {
    max-width: 600px;
    margin: 0 auto;
    text-align: left;
}
.image {
    width: 100%;
    max-width: 600px;
    height: auto;
    display: block;
}
